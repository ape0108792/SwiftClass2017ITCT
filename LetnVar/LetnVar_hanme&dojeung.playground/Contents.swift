//: Playground - noun: a place where people can play

import UIKit

var str = "Hello, playground"

//let은 변경불가능한 상수를 만들고, var는 변경가능한 변수를 만든다.
//하지만 var의 변수타입을 변경할 수는 없다.

let name = "Hanme"
var weight = 70.0

/* name = "Ha Hanme" */
/* let은 값을 바꿀 수 없다. */

weight = 95
/* 이렇게 var는 값을 바꿀 수 있다. */

/* weight = "83" */
/* 하지만 다른 type으로 바꿀 수는 없다. */

let weightLog = [100.0, 95.0, 93.0]
var weightHistory = [70, 75, 68]